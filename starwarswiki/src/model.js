export default {
	currentBrowse: undefined,
	browseResult: {},

	setBrowseResult(params) {
		this.searchResult = params;
	}

	/*addFavorite(path, id) {},
	Firebase stuff
	removeFavorite(path, id) {},*/
};
