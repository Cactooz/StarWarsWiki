export default {
	currentBrowse: undefined,
	browseResult: {},

	setBrowseResult(params) {
		this.browseResult = params;
	}

	/*addFavorite(path, id) {},
	Firebase stuff
	removeFavorite(path, id) {},*/
};
